<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="taxi.flashka.me">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activity.RegisterActivity"
            android:label="@string/sign_up" />
        <activity
            android:name=".activity.ContactsActivity"
            android:label="@string/deal_contacts" />
        <activity
            android:name=".activity.PaymentActivity"
            android:label="@string/payment"/>
        <activity android:name=".activity.DealActivity" />
        <activity
            android:name=".activity.ProfileActivity"
            android:label="@string/deal_profile" />
        <activity
            android:name=".activity.HistoryActivity"
            android:label="@string/deal_history" />
        <activity
            android:name=".activity.WinnerActivity"
            android:label="@string/deal_winners" />
        <activity android:name=".activity.WebActivity"/>
    </application>

</manifest>